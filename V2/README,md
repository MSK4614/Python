# 寻搭子平台 (无数据库版)

## 环境准备
Python >= 3.8

## 安装依赖
pip install -r requirements.txt

## 启动项目
python app.py

## 访问地址
http://127.0.0.1:5000

## 数据存储说明
本项目不使用数据库，数据持久化存储于项目根目录的 `partners.json` 文件中。
首次启动会自动生成该文件及默认测试数据。

# 寻搭子平台 - V1 版本

## 运行方式
1. 确保安装 Python 3.x
2. 安装依赖：`pip install flask`
3. 启动服务：`python app.py`
4. 浏览器访问：`http://127.0.0.1:5000`

## V1 功能清单
- [x] 首页列表展示
- [x] 发布新搭子 (支持标题/地点/时间/分类/描述)
- [x] 详情页查看
- [x] 关键词搜索与分类筛选
- [x] 数据持久化 (保存在 partners.json 中)